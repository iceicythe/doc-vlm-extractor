请从这张收据图片中提取以下字段并输出 JSON：store_name（店铺名）、store_addr（店铺地址）、tel（电话）、date（日期）、time（时间）、subtotal（小计）、tax（税额）、total（总额）。字段名用英文，值保留图中原文。强制规则：只输出你在图片里真实看到的字段；如果某个字段在图中不存在、被遮挡或无法辨认，必须完全不输出该键名，不要输出 null、空字符串或占位符。严禁根据常理推测或编造图中没有的字段——一张图上没有小计，就不要给 subtotal。宁缺毋滥。只输出 JSON，不要任何解释或代码块标记。
