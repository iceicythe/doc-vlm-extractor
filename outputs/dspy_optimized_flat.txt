请从这张收据图片中提取字段并输出 JSON，字段名用英文，值保留图中原文。不要翻译、不要改写、不要纠正拼写。只输出 JSON，不要任何解释或代码块标记。

需要尽可能提取以下所有字段（若图片中确实没有该字段，可省略该字段；但只要图中出现，就应提取）：
- store_name：店铺名
- storeaddr：店铺地址
- tel：电话
- date：日期
- time：时间
- subtotal：小计
- tax：税额
- total：总额

注意事项：
- 必须逐项检查收据的顶部、底部、页脚、边角、细小字区域，很多字段（如地址、电话、时间、小计、税）常出现在这些位置，不要因为不明显就省略。
- 字段值必须严格保留图片中的原始写法，包括大小写、空格、标点、货币符号、连字符、斜杠等。例如店铺名写的是 "Thaivillage"，就不要规范化为 "Thai Village"；写的是 "Restaurant CAVALIERE" 就不要只取一部分或改成别的词。
- 日期和时间要按图中原样提取，不要转换格式。
- 金额要连同货币符号和原格式一起保留，例如 "$47.31"、"€ 32,60"。
- 不要臆造图中不存在的字段；不确定但图中可见的内容应尽量提取，而不是跳过。
- 输出必须是合法 JSON，键名使用上述英文名。
- 只输出 JSON 本身，不要包含解释、注释或 Markdown 代码块标记。

示例输出格式：
{"store_name":"...","storeaddr":"...","tel":"...","date":"...","time":"...","subtotal":"...","tax":"...","total":"..."}
