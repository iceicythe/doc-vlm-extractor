# P0 验证记录

本轮在 `C:\MiniVLM` 完成；未训练、未加载 GPU 模型、未发送真实 API 请求。
Windows 终端测试设置 `PYTHONUTF8=1`；首次未设置时遇到 GBK 输出编码错误，设置后通过。

| 检查 | 结果 |
|---|---|
| 新评分边界测试 `scripts/_test_scoring_v2.py` | 14 个测试方法通过，包含精度、前导零、非有限数、日期、缺失、结构、行序、额外字段、失败分母、共享入口 |
| 原评分回归 `scripts/_test_evaluate_scoring.py` | 全部通过；移除一处 `or True` 造成的无效断言 |
| 本地/API 参数回归 `scripts/_test_evaluate_args.py` | 13 项通过 |
| API 模拟客户端 `scripts/_test_evaluate_api.py` | 39 项通过，未调用真实服务 |
| `src/demo.py --check` | 29 项通过；2,448 条历史预测的展示归一化值与评分核心逐路径相同 |
| 本地 `--score-only` | 微调组 102 条完成，与独立历史审计的计数、指标、样本 GT 哈希一致 |
| 历史旧分数复现 | 两组均与已保存历史 F1 一致，再执行精度诊断与 v2 重算 |
| 历史文件完整性 | 对盘点捕获的 79 个不同预测、汇总、缓存、数据清单文件重新计算哈希，全部未改变 |
| 评分器版本与结果 | 两组最终结果中的源码 SHA-256 均与当前 `src/scoring.py` 相同；逐样本文件各 102 条 |
| Python 语法与修改格式 | 已改入口编译通过，`git diff --check` 通过 |

资产盘点共 33 个实验/产物条目，其中 26 个具备预测字符串和 GT 可进行输出重算，7 个缺少完整恢复条件。
后处理产物的字符串不是原始模型响应证据；它们的 JSON 合法性不能替代原始生成的严格合法率。
本轮正式对比只使用未后处理的零样本和微调组。

偏好质量检查也实际运行：143 对均匹配到 GT；按新版字段 F1，chosen 优于 rejected 为 143 对，
持平和劣于均为 0；chosen 整单正确为 71/143。该结果只用于检查偏好排序，不代表 DPO 效果得到改善。
未修改偏好训练数据，未重新训练。

尚未验证：适配器实际加载、历史提示词完整身份、基座缓存及 GPU 环境；其他实验的新版最终成绩；
OCR 基线和合成陌生版式试点已在后续完成；真实中文单据仍待提供。演示的全局旧成绩及静态截图保留为历史记录，页面和 README 已注明。

正式结果目录：`outputs/evaluation-v2-final/`。`outputs/evaluation-v2-p0/` 是本轮实现期间的中间检查，
其评分器源码哈希已不是最终版本，不作为交付结果。审计脚本拒绝覆盖已有输出目录。

参见 [重算报告](evaluation-v2-report.md)、[资产清单](evaluation-inventory.md)、[评分协议](evaluation-v2.md)。
